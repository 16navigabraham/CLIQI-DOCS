# Narrative Token Launch

CLIQI's AI-powered narrative engine autonomously detects trending crypto narratives on X, launches tokens on Base via Bankr, and distributes them to users. Creator fees are used to buy back both the narrative token and CLIQI.

---

## How It Works

```
X API (dedicated app)          ← Fetch trending crypto tweets
        ↓
Grok / xAI (grok-3-mini)      ← Cluster & score narratives
        ↓
Bankr Deploy API               ← Launch token on Base (gasless)
        ↓
X Bot (@cliqibot)              ← Post launch to X (full CA shown)
Telegram DM                    ← Notify premium users (auto-enabled on TG connect)
        ↓
Users trade the token
        ↓
Bankr creator fees (WETH)      ← 57% of 1.2% swap fee
        ↓
Bankr Agent API                ← Claim fees + accumulate per token + buyback at $100
  ├── 50% WETH → narrative token
  ├── 40% WETH → CLIQI token
  └── 10% WETH → Bankr subscription (reserved)
```

---

## Narrative Detection

### Data Collection

A **dedicated X developer app** (separate from the like-polling app) searches for trending crypto tweets using three query categories:

- General crypto narratives (narrative, meta, trend, alpha)
- Base ecosystem activity (token launches, bullish signals)
- AI + crypto intersection (AI agents, AI tokens)

Tweets are fetched via X API v2 `GET /2/tweets/search/recent` with engagement metrics (`public_metrics`) and author data.

### Scoring (Grok)

Tweets are sent to Grok (`grok-3-mini`) which:

1. Clusters tweets by theme
2. Scores each cluster on:

| Signal | Weight | Description |
|--------|--------|-------------|
| Unique accounts | 30% | Breadth of discussion |
| Engagement velocity | 25% | Likes + RTs per hour |
| Influencer overlap | 20% | Multiple KOLs on same theme |
| Freshness | 15% | Emerged in last 6 hours |
| No existing token | 10% | Not already launched elsewhere |

3. Returns the top narrative with a suggested token name, symbol, and description
4. Only narratives scoring **≥ 70** (configurable) with **≥ 10 unique accounts** qualify

### Quality Gates

- Maximum **1 launch per day**
- Same theme has a **7-day cooldown** (can't launch twice)
- Narrative lifespan is **manually controlled** by admin (`PATCH /api/narratives/:id/complete`)

---

## Token Launch (Bankr)

Tokens are deployed on **Base only** via Bankr's Deploy API:

```
POST https://api.bankr.bot/token-launches/deploy

{
  "tokenName": "AI Agents Meta",
  "tokenSymbol": "AIMETA",
  "description": "The AI agent narrative token launched by CLIQI"
}
```

- **Gasless** — Bankr sponsors all gas fees
- **No upfront cost** — 1.2% swap fee model
- **Creator revenue** — CLIQI earns 57% of swap fees (WETH), paid to the Bankr agent wallet
- Fees go to the Bankr agent wallet by default (no `feeRecipient` set), enabling automated claim + buyback

---

## Fee Buyback

The buyback engine runs as an **hourly SafeInterval** background job.

### Per-Token Accumulation

Since all claimed fees land as WETH in a shared Bankr wallet, fees are attributed per-token using a **balance delta** approach:

1. Snapshot WETH balance **before** claiming
2. Claim fees for the token
3. Snapshot WETH balance **after** claiming
4. `earned = after - before` — only this token's fees
5. Add `earned` to a Redis accumulator: `narrative:weth-accum:{tokenId}`
6. When accumulated WETH (at current ETH price) ≥ **$100** → execute buyback and reset accumulator

This ensures each token's fees are tracked independently regardless of wallet state.

### Accumulation Example

```
Hour 1 — $BAGI
  wethBefore = 0.00, claim, wethAfter = 0.04
  earned = 0.04 WETH → Redis[BAGI] = 0.04 ($86) → below $100, skip

Hour 2 — $BAGI
  wethBefore = 0.04, claim, wethAfter = 0.05
  earned = 0.01 WETH → Redis[BAGI] = 0.05 ($108) → OVER $100
  → buyback immediately, Redis[BAGI] reset to 0
```

### Buyback Split

| Destination | Allocation | Description |
|-------------|------------|-------------|
| Narrative token | 50% | Buy back the active narrative token |
| CLIQI token | 40% | Buy back CLIQI |
| Bankr subscription | 10% | Reserved in wallet (handled externally) |

All swaps execute via Bankr's `/agent/prompt` → `/agent/job/{jobId}` async pattern:

```
"swap 0.025 WETH for 0xNarrativeToken on base"   ← 50%
"swap 0.020 WETH for 0x075b...7ba3 on base"       ← 40%
0.005 WETH stays in wallet                         ← 10%
```

### Multiple Active Tokens

Each active token is processed sequentially. The balance delta per claim ensures tokens don't contaminate each other's accumulators even when WETH from previous runs remains in the wallet.

---

## Distribution

### Primary: X (@cliqibot)

Every launched token is posted to X with:
- Narrative summary
- Score
- Full contract address (CA) + BaseScan link

### Premium: Telegram DM

Premium users receive a Telegram DM automatically when a narrative token launches. Notifications are **auto-enabled** when a user connects their Telegram account and can be toggled individually via `tgNotifyNarrativeLaunch`.

The TG notification includes:
- Token name, symbol, narrative summary, and score
- BaseScan link
- Inline **Buy** button

### Telegram Buy

Preset amounts: **$5 / $10 / $25 / $50 / $100** or custom amount.

The buy respects the user's configured **Base trading mode**:
- `USDC` mode → gasless USDC swap
- `ETH` mode → ETH swap

---

## API Endpoints

### GET /api/narratives

List narrative tokens. Public — no auth required.

**Query params:**

| Param | Type | Description |
|-------|------|-------------|
| status | string | `ACTIVE` or `COMPLETED` |
| limit | number | Max results (default 20, max 50) |

**Response:**
```json
{
  "data": [
    {
      "id": "uuid",
      "narrative": "AI agents are becoming the dominant narrative...",
      "tokenAddress": "0x...",
      "tokenSymbol": "AIMETA",
      "tokenName": "AI Agents Meta",
      "chain": "BASE",
      "status": "ACTIVE",
      "score": 85,
      "tweetId": "1234567890",
      "totalBuybackUsd": 350.00,
      "launchedAt": "2026-03-24T12:00:00Z"
    }
  ]
}
```

### GET /api/narratives/:id

Get narrative detail with buyback history.

### PATCH /api/narratives/:id/complete

**Auth:** Required (admin only)

Manually end a narrative — sets status to `COMPLETED`.

---

## Database Models

### NarrativeToken

| Field | Type | Description |
|-------|------|-------------|
| id | uuid | Primary key |
| narrative | text | AI-generated narrative summary |
| sourceTweets | string[] | Tweet IDs that sparked the narrative |
| tokenAddress | string | Deployed token contract |
| tokenSymbol | string | Ticker |
| tokenName | string | Full name |
| chain | Chain | Always `BASE` |
| bankrTokenId | string? | Bankr activity ID |
| status | NarrativeStatus | `ACTIVE`, `COMPLETED`, `FAILED` |
| score | int | Narrative strength (0–100) |
| tweetId | string? | Posted tweet ID |
| totalBuybackUsd | decimal | Cumulative buyback value |

### NarrativeBuyback

| Field | Type | Description |
|-------|------|-------------|
| id | uuid | Primary key |
| narrativeTokenId | string | FK → NarrativeToken |
| feeSourceUsd | decimal | Total WETH value in USD |
| tokenBuyUsd | decimal | 50% allocation |
| cliqiBuyUsd | decimal | 40% allocation |
| bankrSubUsd | decimal | 10% reserved for Bankr subscription |
| tokenTxHash | string? | Narrative token swap tx |
| cliqiTxHash | string? | CLIQI swap tx |
| status | TxStatus | `PENDING`, `CONFIRMED`, `FAILED` |

---

## Background Jobs

| Job | Interval | Description |
|-----|----------|-------------|
| `narrative-engine` | 30 min | Fetch tweets → score → launch → post |
| `fee-buyback` | 1 hour | Claim fees → accumulate per token → buyback at $100 |

---

## Environment Variables

| Variable | Required | Description |
|----------|----------|-------------|
| `X_NARRATIVE_BEARER_TOKEN` | Yes | Dedicated X API app for narrative scraping |
| `XAI_API_KEY` | Yes | Grok (xAI) API key for narrative scoring |
| `BANKR_API_KEY` | Yes | Bankr API — launch, claim fees, swap |
| `NARRATIVE_SCORE_THRESHOLD` | No | Min score to launch (default: 70) |
| `NARRATIVE_BUYBACK_THRESHOLD_USD` | No | Min accumulated WETH value before buyback (default: $100) |

---

## Revenue Model

```
Token launched via Bankr
        ↓
Users trade on DEX (1.2% swap fee)
        ↓
Fee split:
  ├── 57% → CLIQI (creator)     ← WETH to Bankr agent wallet
  ├── 36.1% → Bankr
  ├── 1.9% → Ecosystem
  └── 5% → Protocol (Doppler)
        ↓
CLIQI's 57% buyback split:
  ├── 50% → buy narrative token  ← price support
  ├── 40% → buy CLIQI token     ← CLIQI price support
  └── 10% → Bankr subscription  ← reserved in wallet
```

Win for all — users get early access to narrative tokens, narrative token gets price support, CLIQI token gets continuous buy pressure.
